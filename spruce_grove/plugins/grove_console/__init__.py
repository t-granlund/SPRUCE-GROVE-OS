"""`/console` -- see and tend the grove sessions running on this machine.

A lightweight local panel: which sessions are open right now, what each one
is working on, and the two things you might want to do about it (restart it
cleanly, or force it down). Co-located with the shell's own session list, so
"which groves are running" has one answer everywhere.
"""
